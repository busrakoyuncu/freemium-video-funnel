# Supabase (Project Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server only. Used to move jobs forward and refund credits. Never expose to the browser.
SUPABASE_SERVICE_ROLE_KEY=

# Render mode. Real rendering is not connected yet, so keep this on.
MOCK_RENDER=true

# Planned, not used yet.
SHOTSTACK_API_KEY=
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=
